#selectLocation.modal.fade{"aria-hidden" => "true", "aria-labelledby" => "myModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %center
          %h4.modal-title Select your Location

      .modal-body
        = select_tag "location", options_for_select(@localities.collect{|l| [l.area_name,l.id]}), class: 'form-control',
        prompt: 'Select the location'

      .modal-footer
        %center
          = button_tag type: 'button', class: 'btn btn-primary manual-loc' do
            %i.fa.fa-check
            Set Location
